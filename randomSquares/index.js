

function setup() {
    createCanvas(windowWidth,windowHeight);
    background(0);
    for(i=0;i<100;i++){
    fill(random(120,160));
    rect(random(width),random(height),random(30),random(100));
    }
  }

  function draw() {
   
    
  }